---
description: "Task list for AST + Symbol Intelligence"
---

# Tasks: AST + Symbol Intelligence

**Input**: Design documents from `/specs/002-ast-symbol-intelligence/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, quickstart.md, contracts/

**Tests**: Included — spec.md's User Stories 3/4 explicitly require malformed-source, unsupported-language, and duplicate-delivery coverage, and plan.md's Tests section commits to contract + integration tests mirroring Feature 001's precedent.

**Organization**: Tasks are grouped by user story (spec.md priorities: US1 P1, US2 P1, US3 P2, US4 P2, US5 P3) to enable independent implementation and testing of each story.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story this task belongs to (US1–US5)
- File paths are exact, matching plan.md's Project Structure

## Path Conventions

Single project (existing TanStack Start app, unchanged layout): `src/lib/code-intel/`, `plugins/`, `data/`, `tests/` at repository root, per plan.md.

---

## Phase 1: Setup

**Purpose**: Add the two new runtime dependencies and the scaffolding every later task needs to exist.

- [X] T001 Add `web-tree-sitter` and `tree-sitter-wasms` to `package.json` dependencies (`bun add web-tree-sitter tree-sitter-wasms`); confirm `bun.lock` records no native/binary transitive dependency (plan.md constraint: "no dependency that cannot run in the deployed Worker"). **Done, with a version correction**: initial `bun add` resolved `web-tree-sitter@0.27.0`, which T012's spike found binary-incompatible with `tree-sitter-wasms@0.1.13`; re-pinned to the verified-compatible `web-tree-sitter@0.25.10` (see research.md §6 risk 1). No native/binary transitive dependency in either version. `tree-sitter-wasms@0.1.13` unchanged.
- [X] T002 [P] Create empty directory scaffolding: `src/lib/code-intel/symbols/`, `src/lib/code-intel/symbols/queries/`, `tests/contract/symbols/`, `tests/integration/symbols/`. **Retroactively verified, no files changed**: all four directories already exist, created implicitly as a side effect of T003 (`symbols/wasm-modules.d.ts`), T005/T008 (`domain/symbol.ts`, `persistence/symbol-d1-client.ts` — different dirs, but T009+ tests populated `tests/contract/symbols/`), and subsequent tasks — this checkbox was simply never flipped once its target state became true by other means. No directory was missing; nothing to create.
- [X] T003 [P] Add ambient TypeScript module declaration for `*.wasm` imports (`src/lib/code-intel/symbols/wasm-modules.d.ts`). **Corrected during T022 remediation (2026-09-20)**: the actually-needed import specifier is `*.wasm?module` (unwasm's `id.endsWith("?module")` branch, confirmed in research.md §6 risk 1), not bare `*.wasm` — a TypeScript wildcard ambient module only matches a literal trailing suffix, so `declare module "*.wasm"` as originally worded would not have covered `*.wasm?module`. Implemented as `declare module "*.wasm?module" { const mod: WebAssembly.Module; export default mod; }`. Validated: `bunx tsc --noEmit` accepts `grammar-provider.ts`'s `import coreWasmModule from "web-tree-sitter/tree-sitter.wasm?module"` with zero errors from this declaration.

**Checkpoint**: Dependencies installed, directories exist, TypeScript accepts `.wasm` imports.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Schema, domain types, persistence layer, queue infrastructure, and the three feasibility spikes — nothing in any user story phase can be implemented correctly before this phase closes.

**⚠️ CRITICAL**: No user story implementation task may begin until this phase (including all three spikes) is complete, per the explicit ordering requirement.

- [X] T004 Append `contracts/d1-schema-additions.sql`'s five `CREATE TABLE` statements (`directories`, `file_extractions`, `symbols`, `extraction_jobs`, `snapshot_extractions`) to `data/code-intel-schema.sql`, additive only — no existing statement in that file is edited. Appended verbatim (`diff` against `contracts/d1-schema-additions.sql`, ignoring header comments: 0 differences). Validated: loaded via `bun:sqlite` (the same mechanism `tests/support/d1-sqlite-adapter.ts` uses) — all 5 new tables present alongside all 7 pre-existing Feature 001 tables, none altered. `bunx tsc --noEmit` clean; Feature 001 regression 31/31; Feature 002 suite 51/51 (unaffected, as expected — no test yet depends on these tables existing).
- [X] T005 [P] Create `src/lib/code-intel/domain/symbol.ts` — `Symbol`, `Directory`, `FileExtraction`, `ExtractionJob`, `SnapshotExtraction` domain types plus `SymbolKind`, `FileExtractionStatus`, `SnapshotExtractionStatus` status enums, per data-model.md (mirrors `domain/snapshot.ts`'s shape). Also defines `ExtractionJobStatus` (`"pending" | "retrying" | "failed" | "completed"`, resolving the naming gap identified in prior analysis — data-model.md's `ExtractionJob.status` values were already authoritative, only the exported type name was undecided). All five types verified field-by-field against data-model.md; models the *persisted* row shapes only (explicit doc comment) — does not attempt to define T028's pre-persistence IR/parent-reference shape, which remains an open design question deliberately deferred, not resolved here. `bunx tsc --noEmit` clean; Feature 001 regression 31/31; Feature 002 full suite 43/43.
- [X] T006 [P] Extend `src/lib/atlas-errors.ts` with two additive `AtlasErrorCode` members: `SNAPSHOT_NOT_EXTRACTABLE`, `SYMBOL_NOT_FOUND` (contracts/extract-symbols.functions.md, contracts/symbol-query.functions.md) — no existing code/message changed. Added both codes to the `AtlasErrorCode` union and `ERROR_MESSAGES` ("Snapshot is not completed and cannot be extracted yet." / "No symbol found for that id.", matching the terse style of existing `SNAPSHOT_NOT_FOUND`/etc — contracts didn't specify exact wording). All 21 existing codes/messages untouched (`diff`-verified). `bunx tsc --noEmit` clean; Feature 002 suite 93/93; Feature 001 regression 31/31 (re-ran 3x to confirm — one single-run failure in `repository-history.test.ts` was a pre-existing timestamp-ordering flake, reproduced independent of this change, passes standalone and on repeat full-suite runs).
- [X] T007 [P] Add `SYMBOL_EXTRACTOR_VERSION` constant and an extraction-batch-size tunable (`CODE_INTEL_EXTRACTION_BATCH_SIZE`) to `src/lib/code-intel/config.ts`'s `codeIntelConfig()`, same two-tier env-var-with-default pattern as existing tunables. **Design note**: `SYMBOL_EXTRACTOR_VERSION` (`"v1"`) is deliberately a plain exported constant, NOT part of `codeIntelConfig()`'s env-overridable return object like the other tunables — per research.md §10, it identifies which version of this repo's own extraction logic produced a result, so an operator overriding it per-deployment would desynchronize the recorded version from what the deployed code actually does, defeating FR-014. `CODE_INTEL_EXTRACTION_BATCH_SIZE` (default 50) follows the standard tunable pattern. No existing constant/field renamed or removed. `bunx tsc --noEmit` clean; Feature 001 regression 31/31; Feature 002 suite 86/86 (unaffected). No dedicated test added — no existing Feature 001 precedent tests `codeIntelConfig()` directly (confirmed: zero references in `tests/`), so none added here either, matching convention.
- [X] T008 Create `src/lib/code-intel/persistence/symbol-d1-client.ts` — thin `D1DatabaseLike` query layer covering: `getOrCreateDirectory`, `upsertFileExtraction`, `replaceSymbolsForFile` (two-pass parent resolution per data-model.md's "Parent/child resolution (IR → D1)" — delete-then-insert with `parent_symbol_id = NULL`, then a second batch resolving `parentSymbolKey` → real `symbols.id` via a `symbolKey → id` map built from `meta.last_row_id`), `upsertExtractionJob`/`markJobCompleted`/`recordJobRetry`/`markJobFailed`/`getJobRetryCount`/`recomputeJobCounters` (mirrors `d1-client.ts`'s `acquisition_jobs` functions), `getOrCreateSnapshotExtraction`, `finalizeSnapshotExtraction`, `getSnapshotExtractionSummary`, `listSymbolsPage`, `getSymbolWithProvenance`, `getFileExtractionRow`. Depends on: T004, T005. **27 real tests** (`tests/contract/symbols/symbol-d1-client.test.ts`, real `bun:sqlite` via `tests/support/d1-sqlite-adapter.ts`, no mocking): `getOrCreateDirectory` idempotency (first-write-wins on `ON CONFLICT DO NOTHING`); `upsertFileExtraction`/`getFileExtractionRow` create+replace-not-duplicate+not-attempted-null-case; `replaceSymbolsForFile` — `parent_symbol_id` verified as a real D1 row id (`typeof === "number"`, not the `symbolKey` string), multi-level (grandparent→parent→child) resolution, re-extraction removes stale symbols for the target file **only** (a sibling file's symbols explicitly asserted untouched), zero-symbol-file clears correctly, same-IR-twice idempotency (content-identical, row ids may differ — expected per data-model.md, not a defect); `listSymbolsPage` pagination/kind-filter/directoryPath-filter/empty-case; `getSymbolWithProvenance` full chain + unknown-id → null; `extraction_jobs` upsert-no-duplicate, no-regression-from-completed, retry-count increment + guard, `markJobFailed`, `recomputeJobCounters` set-not-incremented + retry-safe; `snapshot_extractions` create-on-absence, no-mutate-on-existing, guarded finalization (second call no-op), summary aggregation. Two defects found during test-writing, both traced to the test code itself (not `symbol-d1-client.ts`): a test-helper mistakenly assumed Feature 001's `insertSnapshotFile` returns an id (it returns `void`), and one test's expected row order didn't account for SQLite's ASCII sort (`"W" < "r"`) — both fixed in the test file. Feature 002 suite 78/78; Feature 001 regression 31/31; `bunx tsc --noEmit` clean. One disclosed limitation: the sqlite test adapter's `batch()` runs statements sequentially without simulating a genuine multi-statement transaction failure, so cross-statement atomicity-under-partial-failure was not exercisable with this harness — the ordering/id-capture logic itself was fully exercised.
- [X] T008a [P] Extend `src/lib/code-intel/persistence/cloudflare-env.ts` (additive only) — add `SYMBOL_QUEUE?: QueueLike` to the `CloudflareEnv` type and a `getSymbolQueue(): QueueLike` accessor, mirroring the existing `SNAPSHOT_QUEUE`/`getSnapshotQueue()` pattern exactly (same "throws a clear error if the binding is unavailable outside `wrangler dev`/a deployed Worker" behavior); no existing field, type, or function in this file is renamed, removed, or changed. No dependency — placed here for phase locality (adjacent to T008). Implemented exactly as specified — `getSymbolQueue()` is byte-for-byte structurally identical to `getSnapshotQueue()`, only the binding name/error text differ. T030's `getSymbolQueue()` dependency is now satisfied. `bunx tsc --noEmit` clean; Feature 001 regression 31/31 (Feature 001's own `SNAPSHOT_QUEUE` handling untouched); Feature 002 suite 86/86. No dedicated test added — same no-existing-precedent reasoning as T007.
- [X] T009 [P] Extend `wrangler.toml` with `[[queues.producers]]`/`[[queues.consumers]]` entries for `repo-atlas-symbol-extraction` bound as `SYMBOL_QUEUE`, additive alongside the existing `repo-atlas-snapshot-acquisition`/`SNAPSHOT_QUEUE` entries (research.md §8). Added `[[queues.producers]]` (binding `SYMBOL_QUEUE`, queue `repo-atlas-symbol-extraction`) and matching `[[queues.consumers]]` (`max_batch_size = 10`, `max_retries = 5`, same values as Feature 001's consumer) directly after the existing SNAPSHOT_QUEUE stanzas. Existing SNAPSHOT_QUEUE entries untouched. Validated via `bunx wrangler deploy --dry-run` (after `bun run build` regenerated `.output/server/wrangler.json` from the edited `wrangler.toml`) — bindings table shows both `env.SNAPSHOT_QUEUE (repo-atlas-snapshot-acquisition)` and `env.SYMBOL_QUEUE (repo-atlas-symbol-extraction)` as `Queue` resources. Only `wrangler.toml` modified.
- [X] T010 Create `plugins/cloudflare-symbol-queue.ts` as a **scaffold**: a `cloudflare:queue` Nitro hook listener that checks `batch.queue === "repo-atlas-symbol-extraction"` (ignoring other batches) and imports `processSymbolQueueMessage` from `src/lib/code-intel/queue/symbol-worker.ts` — at this point in the plan (Foundational phase) that module does not exist yet, so this task creates the listener shape with a `// TODO(T030): wire real dispatch once symbol-worker.ts exists` placeholder body, not a working call. Mirrors `plugins/cloudflare-queue.ts`'s shape, does not modify that file. **Actual dispatch wiring happens in T031, after T030.** **Path corrected by explicit user instruction**: T030 already exists at `src/lib/code-intel/symbols/symbol-worker.ts` (not the stale `queue/symbol-worker.ts` this task text names), so this plugin imports the real `processSymbolQueueMessage` directly from the real path — no placeholder body needed, T030's dependency was already satisfied when this task ran. Mirrors `plugins/cloudflare-queue.ts`'s shape (`definePlugin`, `cloudflare:queue` hook, ack/retry per message) with one addition: filters on `batch.queue === "repo-atlas-symbol-extraction"` since both plugins share the same hook and must not double-process each other's batches. `plugins/cloudflare-queue.ts`, `wrangler.toml`, `nitro.config.ts` untouched. `bunx tsc --noEmit` clean; Feature 001 regression 31/31 (`tests/contract/code-intel/`, `tests/integration/code-intel/`). No Feature 002 test added (plugin wiring, not exercisable outside a real Cloudflare Queues consumer — same disclosed gap as `cloudflare-queue.ts`).
- [X] T011 Update `nitro.config.ts`'s `plugins` array to include `./plugins/cloudflare-symbol-queue.ts` alongside the existing `./plugins/cloudflare-queue.ts` entry. Depends on: T010. `plugins` array now `["./plugins/cloudflare-queue.ts", "./plugins/cloudflare-symbol-queue.ts"]` — both entries coexist, neither replaced. `bun run build` succeeded (`web-tree-sitter` now appears in the bundled `_libs` chunk, confirming the new plugin's import chain is reachable); `bunx wrangler deploy --dry-run` bindings table shows both `SNAPSHOT_QUEUE` and `SYMBOL_QUEUE`; `bunx tsc --noEmit` clean. Nothing outside `nitro.config.ts` modified.
- [X] T012 **[SPIKE, blocks T022]** Verify the actually-installed `web-tree-sitter` version's WASM-instantiation API (`Parser.init()` options, `Language.load()` signature) against a Workers-like environment. **Result: `web-tree-sitter@0.27.0` FAILED** (requires `dylink.0` custom section; `tree-sitter-wasms@0.1.13` grammars only carry the legacy `dylink` section — real parse attempt threw `need dylink section`, verified by direct source + `WebAssembly.Module.customSections()` inspection, not docs). **Resolved by re-pinning to `web-tree-sitter@0.25.10`** (last release retaining the `dylink.0`→legacy-`dylink` fallback), verified end-to-end with real, error-free ASTs for all four Tier 1 grammars via `Parser.init({instantiateWasm})` (zero fetch/fs) + `Language.load(Uint8Array)`. Full evidence recorded in `research.md` §6 risk 1. Grammar-byte-sourcing mechanism for the deployed Worker (candidate: Cloudflare `ASSETS` binding) is flagged as T022's concern, not resolved here — no grammar-loading code written.
- [X] T013 **[SPIKE] [P]** Measure per-grammar `.wasm` file sizes shipped by `tree-sitter-wasms` for the four Tier 1 grammars (java/javascript/typescript/tsx) plus `web-tree-sitter`'s core runtime `.wasm`; record the sum against the 64 MiB Worker bundle-size cap as a note appended to `research.md` §6 risk 2. Depends on: T001. **Result: 6,037,023 B ≈ 5.76 MB (~9% of cap) using the verified-compatible `web-tree-sitter@0.25.10` core runtime (205,488 B) + four grammars (430,239 + 647,334 + 2,342,690 + 2,411,272 B). PASS, no bundle-size risk.**
- [X] T014 **[SPIKE] [P]** Construct or source one adversarially large single-file fixture per Tier 1 language family and time a local WASM parse to establish a concrete file-size ceiling recommendation; record the threshold and rationale as a note appended to `research.md` §6 risk 3 — this threshold is consumed by T041. **Result**: real parses (pinned `web-tree-sitter@0.25.10`+`tree-sitter-wasms@0.1.13`) across 10 KB–20 MB, all four Tier 1 grammars — clean, linear, error-free scaling throughout once parsed `Tree`s are freed per-iteration (an initial run without doing so produced a real `RuntimeError: Aborted()` crash at ~5 MB, root-caused to the test harness leaking trees, not an engine limit — itself now a confirmed **implementation requirement for T029**: call `tree.delete()` immediately after each file's symbols are extracted). No hard ceiling justified by the measurements through 20 MB; a defensive ~10 MB circuit-breaker is recommended anyway per the local-timing-vs-Workers-CPU-metering gap (explicitly not claimed to be measured), not because parsing was shown to fail. Full evidence in `research.md` §6 risk 3.

**Checkpoint**: Schema, domain types, persistence layer, queue plumbing, and all three spikes are complete. User story implementation may now begin.

---

## Phase 3: User Story 1 - Structural symbol extraction from a completed snapshot (Priority: P1) 🎯 MVP

**Goal**: Given a completed Feature 001 snapshot, detect language, parse with WASM tree-sitter, extract Module/Class/Interface/Function/Method symbols with source ranges and parent/child nesting, persist deterministically.

**Independent Test**: Trigger `extractSnapshotSymbols` for a completed snapshot containing a Tier 1 language file; confirm the resulting symbol set matches expectations for that file, with source ranges; re-run and confirm identical output (determinism).

### Tests for User Story 1

- [X] T015 [P] [US1] Contract test: `detectLanguage` extension-mapping determinism (every Tier 1 extension + at least one unsupported extension) in `tests/contract/symbols/language-detector.test.ts`. Covers every Tier 1 extension (java/js/jsx/mjs/cjs/ts/mts/cts/tsx), 10 unsupported cases, determinism-across-repeats, TS-vs-TSX distinctness, path-depth independence, and structural arity check. Now GREEN: 27/27 pass following T021's implementation.
- [X] T016 [P] [US1] Contract test: `symbol_key` deterministic hashing — same input always produces the same key, differing snapshot/path/kind/name/position each produce a different key — in `tests/contract/symbols/symbol-identity.test.ts`. Identical-inputs stability, SHA-256-hex shape assertion, and one differs-the-key case each for snapshotId/filePath/kind/name/startLine/startColumn (8 tests total), per data-model.md's Symbol entity exactly. Now GREEN: 8/8 pass following T027's implementation.
- [X] T017 [P] [US1] Contract test: Java fixture (a small `.java` file with a class, an interface, and a method) produces expected symbols + ranges in `tests/contract/symbols/java-extraction.test.ts`. Real grammar/real parse (same `instantiateWasm` mechanism as T028's test), no mocking. Fixture: a class containing a nested interface (with one method) and a sibling method, exercising all three of java.scm's capture kinds (class/interface/method) plus multi-level nesting in one shape. 4 tests: symbol kinds/names, parent/qualifiedName chain (`Greeter` → `Loud` → `shout`, `Greeter` → `greet`), exact 0-indexed source ranges (start/end line, start column) for every symbol, symbolKey uniqueness + parent-key resolution. One self-caught assertion bug (test-authoring, not implementation): first draft expected `qualifiedName` to be `"Loud.shout"` (immediate-parent-only) — actual (correct) behavior per `to-intermediate-representation.ts` is the full ancestor chain, `"Greeter.Loud.shout"`; fixed the assertion, no source touched. `bunx tsc --noEmit` clean; new test 4/4 pass; Feature 001 regression 31/31; Feature 002 suite 107/107 (was 103, +4). No Feature 001/003 file touched.
- [X] T018 [P] [US1] Contract test: JavaScript fixture (a `.js` file with a top-level function and a class with a method) produces expected symbols + ranges in `tests/contract/symbols/javascript-extraction.test.ts`. Mirrors T017's structure exactly. Real grammar/real parse, no mocking. Fixture: one top-level function + a class with one method, exercising javascript.scm's function/class/method captures (this grammar has no interface capture, per the query file's own header comment). 4 tests: symbol kinds/names, nesting (top-level function has no parent/qualifiedName; method's parent is the class, `qualifiedName = "Widget.render"`), exact 0-indexed source ranges for all three symbols, symbolKey uniqueness + parent-key resolution. No self-caught defects this time (T017's qualifiedName-chain lesson applied up front). `bunx tsc --noEmit` clean; new test 4/4 pass first run; Feature 001 regression 31/31; Feature 002 suite 111/111 (was 107, +4). No Feature 001/003 file touched.
- [X] T019 [P] [US1] Contract test: TypeScript + TSX fixtures (a `.ts` file with an interface and a class implementing it, a `.tsx` file with a function component) produce expected symbols + ranges in `tests/contract/symbols/typescript-extraction.test.ts`. Mirrors T017/T018. Fixture: interface `Greeter` + class `EnglishGreeter implements Greeter`, each with its own `greet` method (`method_signature` vs `method_definition`, both correctly captured as kind `method`, disambiguated only by parent/qualifiedName) — plus a separate `.tsx` function-component fixture, asserting zero capture ever matches a `jsx_*` node. 4 tests, 4/4 pass first run. No Feature 001/003 file touched.
- [X] T020 [P] [US1] Integration test: `extractSnapshotSymbols` end-to-end against a completed Feature 001 snapshot fixture; re-run with unchanged `SYMBOL_EXTRACTOR_VERSION` and confirm `listSymbols`'s result set is identical (SC-003, spec US1 Acceptance Scenario 4) in `tests/integration/symbols/extract-symbols.test.ts`. Depended on `listSymbols` (T057) — implemented first this session, unblocking this task. Real end-to-end: `extractSnapshotSymbolsHandler` → synchronous fake-queue drain through the real `processSymbolQueueMessage` → `listSymbolsHandler` → re-run at unchanged version (`reused: true`) → `listSymbolsHandler` again, asserted byte-for-byte `toEqual` the first page (not just equivalent content). One self-caught assertion bug: first draft expected `extractSnapshotSymbolsHandler`'s own return value to already say `status: "completed"` — actual, correct behavior is it reflects the row's state *at the moment captured*, before the synchronous fake queue finishes draining inside `.send()`, so `"in_progress"` is the correct returned value; fixed the assertion, no source touched.

### Implementation for User Story 1

- [X] T021 [US1] Implement `detectLanguage` in `src/lib/code-intel/symbols/language-detector.ts` per contracts/language-grammar-provider.md's extension table. Pure, side-effect-free, path-only (arity 1), extension-lookup-table based — no content sniffing, no Tree-sitter/grammar coupling, no Cloudflare API coupling. All 27 T015 assertions pass; `bunx tsc --noEmit` clean; Feature 001 regression suite unaffected (31/31 pass).
- [X] T022 [US1] Implement `GrammarProvider`/`getParser` in `src/lib/code-intel/symbols/grammar-provider.ts` — `web-tree-sitter` init using T012's verified instantiation call, per-Worker-isolate memoized grammar loading for the four Tier 1 grammars. Depends on: T001, T012. **Grammar-byte-sourcing mechanism resolved (2026-09-20)**: core runtime via Nitro's `?module` ESM WASM import (fed to `Parser.init({instantiateWasm})`, zero fetch/fs); grammars via Cloudflare's `ASSETS` binding (`globalThis.__env__.ASSETS.fetch()`, same per-request binding-stash mechanism `persistence/cloudflare-env.ts` already uses, zero new binding — `ASSETS` is auto-generated by Nitro for this app's existing `public/` dir). Four grammar `.wasm` files added to `public/wasm/` — verified byte-identical (SHA-256) to the pinned `tree-sitter-wasms@0.1.13` package artifacts, and confirmed carried through unchanged into `.output/public/wasm/` by the production build. 8/8 real tests pass (`tests/contract/symbols/grammar-provider.test.ts`: real ASTs for all 4 Tier 1 languages, memoization, cross-language distinctness, defensive rejection) — Bun's own `.wasm?module` resolver returns a bare path string, not a Module (confirmed by direct inspection), so tests inject a `WebAssembly.compile`d core module and local grammar bytes via `setTestCoreWasmModule`/`setTestGrammarBytesSource`; production code path is otherwise identical. **Known limitation, disclosed not hidden**: `grammar-provider.ts` is not yet imported by any reachable route/server-fn (that's T029/T032/T058), so `bun run build` does not itself bundle/exercise this file — the `?module` import mechanism is proven by T012's spike, not by this build. `ASSETS` binding presence and static-asset inclusion in `.output/` were directly confirmed.
- [X] T023 [P] [US1] Write `src/lib/code-intel/symbols/queries/java.scm` — tree-sitter query captures for `@symbol.class`, `@symbol.interface`, `@symbol.function`/`@symbol.method`, plus `@symbol.name` sub-captures, per Java's grammar node types. **Validated against the real pinned grammar** (`tree-sitter-wasms@0.1.13`, via a throwaway `new Query(Java, source)` + `query.matches(tree.rootNode)` check, not just syntax inspection): captures `class_declaration`→`@symbol.class`, `interface_declaration`→`@symbol.interface`, `method_declaration`→`@symbol.method`, each with `name: (identifier)`→`@symbol.name`. Real test fixture (2 classes, 1 interface, 4 methods, 1 constructor) produced exactly the expected 7 matches — constructor correctly excluded (`constructor_declaration` is a distinct node type from `method_declaration` in this grammar). No `@symbol.function`/`@symbol.module` capture — Java's grammar has no free-standing function or module construct (confirmed via `Language.types`), consistent with the task's own scope.
- [X] T024 [P] [US1] Write `src/lib/code-intel/symbols/queries/javascript.scm` — same capture set for JavaScript's grammar node types (function declarations, class declarations/methods; no interface concept in JS). **Validated against the real pinned grammar** (`tree-sitter-wasms@0.1.13`): `function_declaration`→`@symbol.function`, `class_declaration`→`@symbol.class`, `method_definition`→`@symbol.method` (name field is `property_identifier`, not `identifier` — a real, confirmed difference from Java's `method_declaration`), each with `@symbol.name`. Real fixture (1 top-level function, 1 class with constructor/instance-method/static-method, 1 arrow function assigned to a const) produced exactly 5 matches: 1 function, 1 class, 3 methods (constructor included — JS has no separate constructor node type, so this correctly follows the grammar's real structure rather than inventing Java's exclusion) — arrow function correctly excluded (not a `function_declaration`). Zero `@symbol.interface` captures (no interface construct in this grammar, confirmed via `Language.types`). No `@symbol.function`/`@symbol.class`/`@symbol.method` match lacks its `@symbol.name`.
- [X] T025 [P] [US1] Write `src/lib/code-intel/symbols/queries/typescript.scm` — same capture set including `@symbol.interface` for TypeScript's `interface` construct and `@symbol.module` for `namespace`/module declarations where present. **Validated against the real pinned grammar** (`tree-sitter-wasms@0.1.13`, inspected directly, not inferred from Java/JS): real, confirmed finding — `namespace X {}` and `module X {}` parse to **two different node types** (`internal_module` and `module` respectively), both captured as `@symbol.module` per "where present" semantics (both are the grammar's actual namespace/module representation, not an invented one). `interface_declaration`/`class_declaration` name fields are `type_identifier` (a third distinct name-field type, differing from both Java's and JS's). `method_signature` (interface methods, no body) captured alongside `method_definition` (class methods) as `@symbol.method`, since data-model.md's Symbol entity explicitly allows Method under either Class or Interface. Real fixture (namespace, module, interface, class-implementing-interface, 1 constructor + 1 instance method, 1 top-level function) produced exactly the expected 9 matches: 2 module, 1 interface, 1 class, 2 function, 3 method — every match carries `@symbol.name`, `tree.rootNode.hasError === false`.
- [X] T026 [P] [US1] Write `src/lib/code-intel/symbols/queries/tsx.scm` — same capture set as typescript.scm, using the `tsx` grammar (JSX-aware node types). **Independently verified, not copied blind**: direct `Language.types` + real-parse inspection of the pinned `tree-sitter-wasms@0.1.13` `tree-sitter-tsx.wasm` confirmed its declaration node types (`internal_module`/`module`/`interface_declaration`/`class_declaration`/`function_declaration`/`method_definition`/`method_signature`) and their name fields are structurally identical to `typescript.scm`'s — TSX only adds a disjoint JSX node-type set (`jsx_element`, `jsx_opening_element`, etc.) sharing no overlap with any capture pattern. Real fixture (namespace, module, interface with a method, class-implementing-interface with constructor + method, a plain top-level function, and a JSX-returning component function) produced exactly the expected 10 matches: 2 module, 1 interface, 1 class, 3 function, 3 method — the JSX-returning function (`App`) captured exactly once, as `@symbol.function`, via its `function_declaration` alone; zero captures ever matched a `jsx_*`-typed node (explicitly asserted, not assumed). `tree.rootNode.hasError === false`.
- [X] T027 [US1] Implement `src/lib/code-intel/symbols/symbol-identity.ts` — `computeSymbolKey(snapshotId, filePath, kind, qualifiedNameOrName, startLine, startColumn) → string` (SHA-256 hex), per data-model.md. Depends on: T005. Synchronous SHA-256 hex digest of the exact canonical string `${snapshotId} ${filePath} ${kind} ${qualifiedNameOrName} ${startLine} ${startColumn}` (space-joined, no other normalization), via `node:crypto`'s `createHash` — Workers-compatible through this repo's already-enabled `nodejs_compat` compatibility flag, not new infrastructure. `extractor_version` deliberately excluded from the hash. T016 now green (8/8); `bunx tsc --noEmit` clean; Feature 001 regression 31/31; full `tests/contract/symbols/` suite 43/43.
- [X] T028 [US1] Implement `src/lib/code-intel/symbols/to-intermediate-representation.ts` — runs a language's `.scm` query against a parsed tree, walks captures into the common Symbol IR (kind, name, qualifiedName, range, `symbolKey` via T027), and derives `parentSymbolKey: string | null` — the `symbolKey` (not a D1 id) of each matched node's nearest matched ancestor, `null` for a top-level symbol — from the AST ancestor chain (FR-010, not from capture emission order). **`parentSymbolKey` is IR-only and is never persisted as a D1 column** — see data-model.md's "Parent/child resolution (IR → D1)" for how T029 resolves it to `symbols.parent_symbol_id`. Depends on: T023, T024, T025, T026, T027. **Implementation**: two internal passes over `query.matches()`'s captures — pass 1 computes each symbol's `qualifiedName` (dotted ancestor-name chain, walked via real `Node.parent` pointers to the nearest matched ancestors, root-first — the one concrete interpretation data-model.md's own "Outer.Inner.method" example implies; no other algorithm is specified anywhere in the authoritative artifacts, so this is a disclosed interpretation, not an invented requirement) and `symbolKey`; pass 2 resolves `parentSymbolKey` from the nearest ancestor's now-known `symbolKey`, using `Node.id` (unique per-tree) to detect ancestors that are themselves matched symbols. `isExported` is not populated by this IR (none of T023–T026's queries capture export/visibility syntax, so nothing is invented beyond what they establish). 8 new real tests (`tests/contract/symbols/to-intermediate-representation.test.ts`, real grammars/parsing, no mocking): top-level `parentSymbolKey === null` confirmed, nested `parentSymbolKey === parent.symbolKey` confirmed (both single- and multi-level, Java + JavaScript), 0-indexed ranges verified against exact expected line/column, `symbolKey` determinism across two independent parses of identical source confirmed, `symbolKey` change on `snapshotId` change confirmed, sibling-uniqueness + every non-null `parentSymbolKey` resolving to a real IR `symbolKey` confirmed, empty-file → empty IR (no error) confirmed. Feature 002 full suite 51/51; Feature 001 regression 31/31; `bunx tsc --noEmit` clean.
- [X] T029 [US1] Implement `src/lib/code-intel/symbols/extraction-pipeline.ts` — per-file pipeline: (1) derive the file's full directory-path chain (snapshot root down to its immediate parent) from `SnapshotFile.path` and upsert every segment via `symbol-d1-client`'s `getOrCreateDirectory` (FR-006 — runs for every file regardless of extraction outcome, since directory structure is a layout fact, not an extraction-success fact); (2) read bytes via existing `getObject` (`persistence/r2-client.ts`, unmodified); (3) `detectLanguage`; (4) parse via `grammar-provider`; (5) extract via `to-intermediate-representation`; (6) persist via `symbol-d1-client`'s `upsertFileExtraction` + `replaceSymbolsForFile`; with file-scoped try/catch so one file's exception never propagates to another file (FR-007, FR-018). **`replaceSymbolsForFile` is a two-pass write, not a single atomic batch** (data-model.md's "Parent/child resolution (IR → D1)"): pass 1 inserts every IR symbol with `parent_symbol_id = NULL` via `db.batch()`, capturing each row's `meta.last_row_id` to build a `symbolKey → id` map; pass 2 issues a second `db.batch()` of `UPDATE symbols SET parent_symbol_id = ? WHERE id = ?` for every symbol whose `parentSymbolKey` is non-null, resolved through that map. The persisted `Symbol` domain type (T005) is unchanged — `parentSymbolId: number | null` — this is purely a two-write persistence mechanism, not a schema or domain-type change. Depends on: T021, T022, T028, T008. **Implementation notes**: language detection reordered before the R2 read (cheap, path-only — an unsupported file never touches R2 at all, verified explicitly in tests). Malformed source is detected via `tree.rootNode.hasError` (tree-sitter is error-tolerant, rarely throws on garbage input) — a `hasError` tree is treated as a full failure, zero symbols, matching spec Edge Cases' "no symbols are recorded for it." Every parsed `Tree` is `.delete()`'d immediately after IR extraction (T014's finding). Step 1 (directory chain) is deliberately outside the file-scoped try/catch — a directory-upsert failure is treated as an infrastructure fault, not file content, so it's allowed to propagate rather than being recorded as a per-file "failed" status. **New, disclosed addition within T029's own necessity** (same class as T003's `*.wasm?module` declaration): query files are read via Vite/Bun's native `?raw` text-import (`import q from "./queries/java.scm?raw"`, confirmed identical behavior under `bun test` and the production Vite build — no test/production divergence, unlike the WASM case), requiring one small new ambient declaration, `src/lib/code-intel/symbols/scm-modules.d.ts` (`declare module "*.scm?raw"`) — not a separate task, a build-tooling necessity T029 cannot function without. 8 new real integration tests (`tests/integration/symbols/extraction-pipeline.test.ts`, real WASM parsing via grammar-provider's test-injection hooks, real D1 via the sqlite adapter, an in-memory fake R2 bucket): successful extraction with correct directory-chain/symbol persistence, unsupported-language skip (explicitly proven to never touch R2 — key deliberately left unseeded), malformed-source failure (`hasError` path, does not throw), empty-file success with zero symbols, R2-object-missing failure (does not throw), nested parent/child `parent_symbol_id` resolved end to end to a real D1 id, re-extraction replaces (not duplicates) both `file_extractions` and `symbols`, `extractor_version` stamped correctly on both tables. Feature 002 suite 86/86; Feature 001 regression 31/31; `bunx tsc --noEmit` clean.
- [X] T030 [US1] Implement `symbol-worker.ts` — `processSymbolQueueMessage`: one `ExtractionJob` unit per invocation, iterates its assigned file batch through `extraction-pipeline.ts`, enqueues the next unit (via `getSymbolQueue()`, T008a) or finalizes `snapshot_extractions` when the last batch completes (structurally mirrors `queue/snapshot-worker.ts`). Depends on: T029, T008, T008a. **Path deviation, explicit**: created at `src/lib/code-intel/symbols/symbol-worker.ts` (per direct instruction), not `src/lib/code-intel/queue/symbol-worker.ts` as this line originally specified — flagged once, then followed the repeated explicit instruction. **Message shape extended**: `SymbolQueueMessage` carries a third field, `fromCursor: number` (this unit's assigned starting point in the snapshot's `snapshot_files`, ordered by id), needed for the multi-unit chunking scheme data-model.md's "Checkpoint/Resume mechanism" describes but doesn't specify how a unit learns its own range boundary — threaded through the queue message rather than invented as new schema. **Strict layering preserved** (explicit focus of this task): the worker contains zero parsing/IR/symbol-persistence logic — every file is handed to `extraction-pipeline.ts`'s `extractFile` unchanged; the worker only decides which file batch to hand it and manages `extraction_jobs`/`snapshot_extractions` lifecycle. Two small **local, unexported** read-helpers (`getSnapshotExtractionState`, `getExtractionJobState`) were added inside `symbol-worker.ts` itself rather than re-opening the already-closed T008 (`symbol-d1-client.ts`) — these read lifecycle *status* columns for the worker's own control-flow decisions (terminal-check, idempotent-skip, resume), not symbol/file content, so they stay local rather than becoming part of T008's public persistence API. 7 new real integration tests (`tests/integration/symbols/symbol-worker.test.ts`, real WASM parsing, real D1, fake R2 + fake queue): completed extraction (small snapshot, one unit), partial failure → `completed_partial`, duplicate delivery → no duplicate symbols/no re-finalization, already-terminal snapshot → no-op, **resume** (pre-seeded checkpoint + sentinel `extractor_version` on already-processed files, proven untouched after resume — only files past the checkpoint reprocessed), **retry** (injected transient D1 failure → `retrying` status + `retry_count` increment + rethrow for queue redelivery, then a real retry succeeds), multi-unit enqueue (forced small batch size, verifies `getSymbolQueue().send()` called with the correct next `unitIndex`/`fromCursor`, snapshot not yet finalized). Feature 002 suite 93/93; Feature 001 regression 31/31; `bunx tsc --noEmit` clean.
- [X] T031 [US1] Replace T010's `// TODO(T030)` placeholder in `plugins/cloudflare-symbol-queue.ts` with a real call to the now-implemented `processSymbolQueueMessage` (T030) — this is the task that completes actual dispatch wiring; T010 only ever scaffolded the listener shape. Depends on: T010, T030. **No placeholder existed to replace**: by the time T010 ran, T030 was already `[X]` (real `symbol-worker.ts` existed), so T010 wrote the real dispatch call directly instead of a TODO stub — verified by re-reading `plugins/cloudflare-symbol-queue.ts` (no `TODO`/`placeholder` text present, `processSymbolQueueMessage` called for each message with ack/retry). T031 is therefore already satisfied; no code change made. Re-verified: `bunx tsc --noEmit` clean, `bun run build` succeeds, Feature 001 regression 31/31.
- [X] T032 [US1] Implement `extractSnapshotSymbols` (plain handler + `createServerFn` wrapper) in `src/lib/code-intel/symbol.functions.ts` per contracts/extract-symbols.functions.md — verifies Feature 001 snapshot is `completed` (else throws `SNAPSHOT_NOT_EXTRACTABLE`), resolves existing `snapshot_extractions` state, creates/reuses as specified, enqueues the first unit via `getSymbolQueue()` (T008a). Depends on: T006, T008, T008a, T030. Implements exactly the 5 contract behaviors (not-completed → error, no-row → create+enqueue `reused:false`, same-version completed/completed_partial → `reused:true` no enqueue, in_progress concurrent request → returned as-is no duplicate enqueue, version-bump → fresh run `reused:false`). **Two small necessary additions to `symbol-d1-client.ts`** (T008, already closed), disclosed: `getSnapshotExtractionRow` (read-only peek, since `getOrCreateSnapshotExtraction` always creates-on-absence and this handler must inspect existing state *before* deciding reuse/restart) and `restartSnapshotExtraction` (resets the row to fresh `in_progress` under the current `SYMBOL_EXTRACTOR_VERSION` **and clears `extraction_jobs` for the snapshot** — required because `upsertExtractionJob`'s `ON CONFLICT` preserves a `'completed'` unit status, which would otherwise cause a version-bump restart's unit 0 to be silently skipped as already-done, defeating FR-014). **One undisclosed-by-contract case resolved by elimination**: an existing row with `status: 'failed'` under the *same* extractor version falls through to the restart branch (same as "no row"/"version differs") since it doesn't satisfy either the reuse or in-progress conditions — not explicitly listed in the 5 contract behaviors, flagged here rather than silently assumed. 8 new integration tests (`tests/integration/symbols/extract-snapshot-symbols.test.ts`, real `bun:sqlite`, fake queue): not-completed throws, unknown-id throws, no-row creates+enqueues, same-version completed/completed_partial reuse (both statuses), in-progress concurrent no-duplicate, version-bump restart+enqueue, and a dedicated test proving the stale-`extraction_jobs`-clearing behavior (pre-seeded `unit_index=0` row with `status='completed'` under the old version is gone after restart). Feature 002 suite 101/101; Feature 001 regression 31/31 (one single-run flake in `repository-history.test.ts` on a repeat run — same pre-existing timestamp-ordering issue reported in T006/T030, reproduced independent of this change); `bunx tsc --noEmit` clean.

**Checkpoint**: User Story 1 is independently functional — triggering extraction on a completed snapshot produces persisted, deterministic Module/Class/Interface/Function/Method symbols for all Tier 1 languages.

---

## Phase 4: User Story 2 - Symbol identity, source ranges, and provenance (Priority: P1)

**Goal**: Every extracted symbol is retrievable by a stable identity with its exact source range and a full provenance chain to file/snapshot/repository/commit SHA.

**Independent Test**: Extract symbols for a snapshot; read a symbol back twice and confirm identical identity; query its provenance and confirm it resolves to exactly one file/snapshot/repository/commit SHA in one call.

### Tests for User Story 2

- [X] T033 [P] [US2] Contract test: `getSymbol` resolves full provenance (file path, snapshot id, repository, commit SHA) in one call, and throws `SYMBOL_NOT_FOUND` for an unknown id, in `tests/contract/symbols/symbol-provenance.test.ts`. Satisfied by the test file written during T035's own validation (same path the task specifies) — verified against contracts/symbol-query.functions.md's `SymbolDetail` shape field-by-field (exact match, including nested `provenance.repository`), both scenarios covered (full-chain resolution, unknown-id throw). No implementation touched this turn. Feature 002 suite 103/103; Feature 001 regression 31/31 (clean, no flake); `bunx tsc --noEmit` clean.
- [X] T034 [P] [US2] Integration test: a symbol's identity and detail are unchanged when read in a separate later invocation (fresh D1 connection/test run) **as long as `extractor_version` is unchanged** (spec FR-008/US2 Acceptance Scenario 1, as scoped in spec.md Assumptions), and two symbols from different snapshots never share an identity even for "the same" logical declaration (spec US2 Acceptance Scenario 4), in `tests/integration/symbols/symbol-identity-stability.test.ts`. Explicitly does NOT assert identity survives an `extractor_version` bump — that is covered separately by T047, where new identities are the expected, correct outcome. 2 real integration tests (real WASM parse, real D1, no mocking), distinct from T027's unit-level `computeSymbolKey` determinism test and T028's within-one-run uniqueness test: two genuinely separate `extractFile` invocations of identical content/version produce an identical `symbol_key`; two different snapshots of identical source content never collide. 2/2 pass first run.

### Implementation for User Story 2

- [X] T035 [US2] Implement `getSymbol` (plain handler + `createServerFn` wrapper) in `src/lib/code-intel/symbol.functions.ts` per contracts/symbol-query.functions.md — joins `symbols` → `file_extractions` → `snapshot_files`/`snapshots` → `repositories` for the full provenance chain in one D1 round trip via `getSymbolWithProvenance` (T008); throws `SYMBOL_NOT_FOUND` when absent. Depends on: T006, T008, T032 (same file). Thin reshape only — `getSymbolWithProvenance`'s flat row mapped into the contract's nested `provenance` object, zero SQL/JOIN logic in the handler (T008 untouched). Added to `symbol.functions.ts` alongside `extractSnapshotSymbols` (T032, same file). **T033's contract test file written as part of this task's own validation** (`tests/contract/symbols/symbol-provenance.test.ts` — the exact path/name T033 specifies), since T033 couldn't compile until `getSymbol` existed (flagged in the prior investigation turn) — 2 tests: full-provenance-in-one-call (exact shape match against contract's `SymbolDetail`), unknown-id throws `SYMBOL_NOT_FOUND`. **T033 itself is left unmarked here** — this task's own validation happened to produce the same file T033 calls for, but T033 wasn't separately authorized this turn; flagging for explicit confirmation rather than silently closing it. Feature 002 suite 103/103; Feature 001 regression 31/31 (clean this run, no flake); `bunx tsc --noEmit` clean.

**Checkpoint**: User Stories 1 and 2 both work independently — symbols are extracted, identified, and fully traceable.

---

## Phase 5: User Story 3 - Graceful handling of unsupported languages and malformed source (Priority: P2)

**Goal**: Unsupported-language files are recorded as skipped, malformed/oversized files are recorded as failed (file-scoped), and snapshot-level status honestly reflects partial coverage.

**Independent Test**: Run extraction against a snapshot with a supported file, an unsupported-language file, and a malformed file; confirm the supported file's symbols exist, the others are recorded as skipped/failed respectively, and overall status is `completed_partial`.

### Tests for User Story 3

- [X] T036 [P] [US3] Contract test: a file with an unsupported extension is recorded `file_extractions.status = 'skipped_unsupported'`, contributes zero symbols, does not block sibling files, in `tests/contract/symbols/unsupported-language.test.ts`. **Satisfied by existing coverage, no new file** — T029's own `tests/integration/symbols/extraction-pipeline.test.ts` already has this exact scenario (`"unsupported language: skipped, never reads R2, no symbols"`), written before T036 was reached, since T029's own acceptance required proving it. Verified the test still exists and passes; not duplicated under a second filename.
- [X] T037 [P] [US3] Contract test: a syntactically malformed Tier 1 file is recorded `status = 'failed'` with a non-null `failure_reason`, contributes zero symbol rows, does not block sibling files, in `tests/contract/symbols/malformed-source.test.ts`. **Satisfied by existing coverage, no new file** — same file, `"malformed source: syntax errors recorded as failed, zero symbols, does not throw"` (T029). Verified still passing.
- [X] T038 [P] [US3] Integration test: quickstart.md's US3 mixed-snapshot scenario (valid + unsupported + malformed) end-to-end — `getExtractionStatus` reports `completed_partial` with correct `filesSkippedUnsupported`/`filesFailed` counts (SC-004), in `tests/integration/symbols/partial-coverage.test.ts`. New file, new tests (this was genuinely new — `getExtractionStatus`/`getFileExtraction` didn't exist before T043/T044 below). 4 tests through the real queue worker (`processSymbolQueueMessage`), read back via the public query surface, not raw SQL: `completed_partial` with exact file-outcome counts (1 extracted/1 skipped/1 failed, 1 symbol), `not_started` for a snapshot with no extraction row, per-file `getFileExtraction` outcomes for all three files, `not_attempted` for a never-processed path. 4/4 pass.

### Implementation for User Story 3

- [X] T039 [US3] Implement the unsupported-language path in `extraction-pipeline.ts`: when `detectLanguage` returns `null`, write `file_extractions` with `status='skipped_unsupported'`, `language=null`, no parse attempted (FR-003). Depends on: T029. **Already implemented as part of T029** (same file, same task boundary in practice) — verified present, unchanged, retroactively checked off; no code touched this turn.
- [X] T040 [US3] Implement the malformed-source/parse-failure path in `extraction-pipeline.ts`: catch parse/extraction exceptions, write `file_extractions` with `status='failed'` + `failure_reason`, ensure zero `symbols` rows are committed for that file (FR-018). Depends on: T029. **Already implemented as part of T029** — same as T039, verified present, retroactively checked off.
- [X] T041 [US3] Implement the oversized-file fallback in `extraction-pipeline.ts` using T014's spike-derived size threshold: a file whose byte length exceeds the threshold is recorded `status='skipped_unsupported'`-analogous (or a distinct `failure_reason` noting "exceeds size ceiling") without attempting a parse. Depends on: T014, T029. **Genuinely new this turn**: added `CODE_INTEL_MAX_FILE_SIZE_BYTES` (10 MB, T014's recommended defensive circuit-breaker — no hard failure point was measured through 20 MB, this is the disclosed-as-precautionary ceiling) to `config.ts`'s `codeIntelConfig()`, and a size gate in `extraction-pipeline.ts` right after language detection (cheap, path-only — `SnapshotFile.sizeBytes` is Feature 001's own already-recorded inventory metadata, so this never requires an R2 read to decide). Uses `status: 'skipped_unsupported'` with a distinct `failureReason` string noting the size ceiling, per the task's own "or a distinct failure_reason" alternative. `bunx tsc --noEmit` clean; Feature 002 suite unaffected (no existing test uses a >10 MB fixture).
- [X] T042 [US3] Implement snapshot-level status rollup in `symbol-worker.ts`'s finalization step: `completed` iff every `file_extractions` row for the snapshot is `extracted`; `completed_partial` iff at least one is `skipped_unsupported`/`failed` but the run otherwise finished; `failed` iff the extraction run itself could not finish (FR-016). Depends on: T030, T039, T040. **Already implemented as part of T030** — verified present (`finalStatus` computation in `processSymbolQueueMessage`), retroactively checked off, no code touched.
- [X] T043 [US3] Implement `getExtractionStatus` (plain handler + `createServerFn` wrapper) in `symbol.functions.ts` per contracts/extract-symbols.functions.md — returns `not_started` (no row, never throws) through `completed_partial`/`failed` plus file/symbol counts. Depends on: T008, T042. **Genuinely new this turn**: thin reshape only — `getSnapshotExtractionRow`/`getSnapshotExtractionSummary` (both already existed from T008) supply every field; zero new SQL. Tested via T038's `partial-coverage.test.ts`.
- [X] T044 [US3] Implement `getFileExtraction` (plain handler + `createServerFn` wrapper) in `symbol.functions.ts` per contracts/symbol-query.functions.md — returns `not_attempted` (no row, never throws) through `extracted`/`skipped_unsupported`/`failed`. Depends on: T008. **Genuinely new this turn**: thin reshape of T008's already-existing `getFileExtractionRow`, `not_attempted` fallback added for the no-row case. Tested via T038's `partial-coverage.test.ts`.

**Checkpoint**: Real-world heterogeneous repositories produce honest, non-silent partial-coverage results.

---

## Phase 6: User Story 4 - Asynchronous, checkpointed, idempotent processing (Priority: P2)

**Goal**: Large snapshots process through bounded queue units; duplicate delivery, retries, and re-extraction never corrupt or duplicate data.

**Independent Test**: Simulate a duplicate queue-message delivery; confirm identical resulting symbol data to a single delivery. Simulate a mid-unit failure and confirm resumption via checkpoint without reprocessing already-extracted files.

### Tests for User Story 4

- [X] T045 [P] [US4] Contract test: redelivering an already-processed `ExtractionJob` message produces no duplicate `symbols`/`file_extractions` rows (SC-005), in `tests/contract/symbols/duplicate-delivery.test.ts`. **Satisfied by existing coverage, no new file** — `tests/integration/symbols/symbol-worker.test.ts` (T030) already has `"duplicate delivery: processing the same completed unit again is a no-op, no duplicate symbols"`. Verified still present and passing.
- [X] T046 [P] [US4] Contract test, split across the two actually-reachable idempotency paths (data-model.md's "Re-extraction mechanism" — the public API does NOT re-run extraction for an unchanged `extractor_version`, see below): (a) calling `extractSnapshotSymbols` a second time against an already-`completed`/`completed_partial` snapshot at the same `SYMBOL_EXTRACTOR_VERSION` returns `reused: true` and performs zero D1 writes (assert row count on `symbols` and content equality on `file_extractions` state as appropriate); (b) directly exercising `symbol-d1-client`'s `replaceSymbolsForFile` at the persistence layer with an unchanged symbol set confirms the delete-then-insert path produces an identical resulting row set (content-equal, independent of new row `id`s). In `tests/contract/symbols/re-extraction-idempotent.test.ts`. **Satisfied by existing coverage, no new file**: (a) `extract-snapshot-symbols.test.ts`'s `"existing completed extraction, same version: reused, no new work"`-class tests, now further exercised end-to-end by this turn's T020 (`listSymbols` byte-identical across the reused call); (b) `symbol-d1-client.test.ts`'s `"idempotency: re-running with the identical IR twice produces content-identical results"` (T008). Both verified still present and passing.
- [X] T047 [P] [US4] Contract test: bumping `SYMBOL_EXTRACTOR_VERSION` and re-running extraction produces a distinguishable new result (new `extractor_version` recorded on every affected row, FR-014), in `tests/contract/symbols/re-extraction-version-bump.test.ts`. **Satisfied by existing coverage, no new file** — `extract-snapshot-symbols.test.ts`'s `"version-bump restart clears stale extraction_jobs so unit 0 isn't skipped as already-completed"` (T032). Verified still present and passing.
- [X] T048 [US4] Integration test: a snapshot large enough to require ≥2 `ExtractionJob` units — interrupt/force-fail mid-unit, confirm `getExtractionStatus` never reports `completed`/`completed_partial` until every unit genuinely finishes, then confirm successful resumption via `checkpoint_cursor` skips already-extracted files (mirrors Feature 001 US6/SC-006), in `tests/integration/symbols/checkpoint-resume.test.ts`. **Satisfied by existing coverage, no new file** — `symbol-worker.test.ts`'s `"resume: files already covered by checkpoint_cursor are not re-queried; only files past it are processed"` and `"enqueues the next unit... when more files remain beyond the batch size"` (both T030), which together cover both halves of this task (never-finalize-until-every-unit-done is implicit in the multi-unit test's `status: "in_progress"` assertion). Verified still present and passing.
- [X] T049 [US4] Integration test: two concurrent `extractSnapshotSymbols` requests for the same snapshot do not enqueue duplicate unit sequences (Edge Cases), in `tests/integration/symbols/concurrent-extraction-request.test.ts`. **Satisfied by existing coverage, no new file** — `extract-snapshot-symbols.test.ts`'s `"existing in_progress extraction (concurrent request): returns in_progress, no duplicate enqueue"` (T032). Verified still present and passing.

### Implementation for User Story 4

- [X] T050 [US4] Implement idempotent per-file skip-if-already-extracted check (keyed on `snapshot_id` + `snapshot_file_id` + current `extractor_version`) and `checkpoint_cursor` advancement (last-covered `snapshot_files.id`) in `symbol-worker.ts`'s batch-iteration loop. Depends on: T029, T030. **Already implemented as part of T030** — `checkpoint_cursor` is advanced after every file (`upsertExtractionJob(..., String(file.id), ...)`) and `resumeCursor = max(fromCursor, existingCursor)` skips everything already covered. Verified present, retroactively checked off, no code touched.
- [X] T051 [US4] Implement unit-level retry/failure handling in `symbol-worker.ts` — mirrors `snapshot-worker.ts`'s `handleUnitFailure`: increment `retry_count` up to `codeIntelConfig().maxRetryAttempts`, mark the `ExtractionJob` `retrying`→`failed`, and on permanent failure mark `snapshot_extractions.status = 'failed'`. Depends on: T030, T007. **Already implemented as part of T030** — `handleUnitFailure` verified present, matches spec exactly. Retroactively checked off, no code touched.
- [X] T052 [US4] Implement the concurrent-request guard in `extractSnapshotSymbols`: an `in_progress` `snapshot_extractions` row is returned as-is with `reused: false` and no new units are enqueued (contract scenario 4). Depends on: T032. **Already implemented as part of T032, but with a discrepancy flagged rather than silently resolved**: this task's own text specifies `reused: false` for the concurrent/in-progress case; the actual, already-implemented, already-tested (T032/T049) behavior returns `reused: true`. The contract file itself (`extract-symbols.functions.md` scenario 4) does not state a `reused` value for this case at all — only scenario 3 (same-version completed reuse) explicitly specifies `reused: true`. `reused: true` is semantically consistent with the field's own doc comment ("true when an existing extraction... was returned instead of starting a new one") and is what T032's and T049's passing tests assert (`extract-snapshot-symbols.test.ts`: `"existing in_progress extraction (concurrent request): returns in_progress, no duplicate enqueue"` asserts `reused: true`). Per this session's goal instructions ("if a task description conflicts with contracts/, stop and report" — this is a task-text-vs-implementation conflict, not a contract violation), **not changing already-shipped, already-tested behavior to match this task's stale prose** — flagging for explicit human confirmation instead. No code touched this turn.
- [X] T053 [US4] Wire the extraction-batch-size tunable (T007) into `symbol-worker.ts`'s unit-sizing logic (how many files one `ExtractionJob` unit covers). Depends on: T007, T030. **Already implemented as part of T030** — `listSnapshotFilesPage(snapshotId, resumeCursor, config.extractionBatchSize, database)` verified present. Retroactively checked off, no code touched.

**Checkpoint**: Extraction is safe under Cloudflare Queues' at-least-once delivery and safely resumable for large snapshots.

---

## Phase 7: User Story 5 - Queryable symbol retrieval (Priority: P3)

**Goal**: A later feature or operator can list/filter/page a snapshot's symbols and retrieve full symbol detail without re-running extraction, over the deployed HTTP RPC surface.

**Independent Test**: For a snapshot with completed extraction, list symbols filtered by kind in bounded pages; retrieve one symbol's full detail directly by id.

### Tests for User Story 5

- [X] T054 [P] [US5] Contract test: `listSymbols` never returns more than the requested/configured `limit` per page, in `tests/contract/symbols/list-symbols-pagination.test.ts`. New file, genuinely new (handler didn't exist before T057). `listSymbolsPage`'s own pagination mechanics already have full contract coverage from T008 (27 tests); this covers `listSymbols`'s own added behavior — config-clamped `limit` (requested-limit respected, configured-max-limit clamps a larger request, default applies when none given). 3/3 pass.
- [X] T055 [P] [US5] Contract test: `listSymbols` correctly filters by `kind` and by `directoryPath`, and returns an empty page (never throws) for a snapshot with no `snapshot_extractions` row, in `tests/contract/symbols/list-symbols-filtering.test.ts`. New file. `kind`/`directoryPath` filtering exercised through the real handler against a real extracted snapshot; empty-page-never-throws gate tested against a snapshot with no `snapshot_extractions` row at all. 3/3 pass.
- [X] T056 [US5] Integration test: quickstart.md's US5 scenario — page through a snapshot's full symbol set across multiple `listSymbols` calls using `nextCursor`, confirm the union matches the complete extracted set with no duplicates or omissions, in `tests/integration/symbols/list-symbols-paging.test.ts`. New file. 11 real symbols across 11 files, paged 4-at-a-time via real `nextCursor` chaining until exhausted; asserts zero duplicates, zero omissions, and that multiple pages were genuinely exercised (not a single-page false pass). 1/1 pass.

### Implementation for User Story 5

- [X] T057 [US5] Implement `listSymbols` (plain handler + `createServerFn` wrapper) in `symbol.functions.ts` per contracts/symbol-query.functions.md — cursor-based pagination reusing `snapshot_files.id`-style ordering (data-model.md, Checkpoint/Resume), `kind`/`directoryPath` filtering, empty-page-never-throws for an unstarted extraction. Depends on: T008. Thin reshape only, zero new SQL — `listSymbolsPage` (T008) already implemented the exact contract shape; the handler adds only the "no `snapshot_extractions` row → empty page" gate and config-clamped `limit` (reused Feature 001's existing `listFilesDefaultLimit`/`listFilesMaxLimit` tunables rather than inventing new symbol-specific ones, disclosed reuse — no separate limit concept exists anywhere else in this codebase to justify a new pair). This was the blocking dependency for T020 — implemented first, then T020 built on it. `bunx tsc --noEmit` clean.
- [X] T058 [US5] Create `src/lib/code-intel/feature-002-server-fn-registration.tsx` — `useServerFn` on all five functions (`extractSnapshotSymbols`, `getExtractionStatus`, `listSymbols`, `getSymbol`, `getFileExtraction`), mirroring `feature-001-server-fn-registration.tsx`'s null-component pattern, deliberately registering every function (research.md §9 — closes Feature 001's observed HTTP-registration gap). Depends on: T032, T035, T043, T044, T057. Byte-for-byte mirrors `feature-001-server-fn-registration.tsx`'s structure (null component, `useServerFn` per function, doc-comment explaining why). `bunx tsc --noEmit` clean; `bun run build` succeeds.
- [X] T059 [US5] Mount `Feature002ServerFnRegistration` in `src/routes/__root.tsx` alongside the existing `Feature001ServerFnRegistration` (one new import + one new JSX line; no other change to that file). Depends on: T058. Exactly one import line + one JSX line added, immediately after `Feature001ServerFnRegistration`'s existing usage. No other line in `__root.tsx` touched. `bunx tsc --noEmit` clean; `bun run build` succeeds.

**Checkpoint**: All five user stories are independently functional; every server function is HTTP-reachable.

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: Whole-repository regression checks and production/live validation, matching Feature 001's own closure pattern.

- [X] T060 [P] Add a header comment to `data/code-intel-schema.sql` noting the Feature 002 section boundary (documentation only, no behavior change). **Already satisfied as part of T004** — lines 80–83 already carry exactly this header ("AST + Symbol Intelligence (specs/002-ast-symbol-intelligence) — additive... No existing statement above this point is altered"). Verified present, retroactively checked off, no file touched this turn.
- [X] T061 Run `bunx tsc --noEmit` across the whole repository — confirm zero type errors, including the new `.wasm` ambient module declaration (T003) and all new files (FR-026/FR-027 regression guard). Clean, zero errors.
- [X] T062 Run the full `bun test` suite (existing Feature 001 tests + all new `tests/contract/symbols/` and `tests/integration/symbols/` tests) — confirm zero regressions (SC-007). Whole-repo `bun test`: **198 pass, 0 fail, 487 expect() calls, across 37 files** — run twice back-to-back, identical result both times (no flake). Breaks down as Feature 001 31/31 + Feature 002 129/129 (was 103 at session start — 26 new tests added this session across T017–T020, T034, T038, T054–T056) + `tests/unit/`+`tests/integration/repositories/` 38/38.
- [X] T063 Production build validation: `bun run build`; inspect `.output` bundle size, resolving T013's spike measurement against the real, fully-implemented bundle (not just the raw grammar files) versus the 64 MiB Worker cap; record the result in `research.md` §6 risk 2 or flag a follow-up if the cap is threatened. Build succeeds. `.output/server` (the actual Worker script) totals ≈5.1 MB (~8% of the 64 MiB cap) with `web-tree-sitter.mjs` and `symbol.functions-*.mjs` now genuinely bundled/reachable (confirms T022's disclosed "not yet exercised by a build" caveat is resolved by T058/T059's registration). The four grammar `.wasm` files (≈7.2 MB) are static assets in `.output/public/wasm/`, a separate size class from the Worker script cap. Recorded in `research.md` §6 risk 2. Cap not threatened — no follow-up needed.
- [X] T064 **PERFORMED — real live Cloudflare infrastructure, and it surfaced a genuine, reproducible production defect. Not silently fixed — reported per this session's "stop and report, don't invent architecture changes" rule, since this touches the T012/T022 WASM-loading decision.** A real Cloudflare account *was* available (user corrected an earlier wrong assumption that it wasn't) — user explicitly authorized live provisioning + deploy. Performed for real:
  - Applied full schema to production D1 (`repo-atlas-code-intel`, `wrangler d1 execute --remote`) — 11 tables now live (previously 0).
  - Created the real `repo-atlas-symbol-extraction` queue (`wrangler queues create`).
  - Built and deployed the Worker for real (`bunx nitro deploy --prebuilt`) to `https://repo-atlas.imdadareeph.workers.dev` — confirmed all 5 bindings attached (DB/SNAPSHOTS/SNAPSHOT_QUEUE/SYMBOL_QUEUE/ASSETS).
  - Ran real Feature 001 `acquireSnapshot` end-to-end against two real public GitHub repos (`octocat/Hello-World`, `sindresorhus/pify`, `sindresorhus/is-plain-obj`) — real GitHub codeload, real R2 storage, real D1 writes, real completion (confirms Feature 001's own live gate — never previously exercised end-to-end — for the first time).
  - Ran real `extractSnapshotSymbols` against those live snapshots, drained through the real deployed queue consumer.
  - **Finding (CRITICAL, reproducible across 2 independent snapshots, 10 total Tier-1 files)**: every real `.js`/`.ts` file fails with `"Cannot read properties of undefined (reading 'href')"`, recorded correctly as `file_extractions.status = 'failed'` (the file-scoped containment from T029/FR-018 works exactly as designed — one file's failure doesn't block siblings or crash the worker), but **zero files ever reach `status = 'extracted'`** in the real deployed environment. Confirmed via read-only `wrangler d1 execute --remote` queries against `file_extractions`, not just the `getExtractionStatus` summary. `wrangler tail` showed no uncaught exception (expected — the error is caught and recorded, by design, so tail alone can't diagnose it; the D1 query was the actual diagnostic tool).
  - **Root cause (read-only investigation, no fix attempted)**: `grammar-provider.ts`'s `ensureCoreInitialized()` supplies a custom `instantiateWasm` to `Parser.init()`, intended to make `web-tree-sitter`'s core-runtime WASM load zero-fetch (research.md §1). But `.output/server/_libs/web-tree-sitter.mjs` (the vendored Emscripten glue, not this repo's code) still contains its own default `locateFile` fallback (`new URL("tree-sitter.wasm", import.meta.url).href`) — evidence that some code path inside `web-tree-sitter`'s real generated glue is not actually short-circuited by our custom `instantiateWasm` the way T012's isolated Bun-only spike (which fed a pre-compiled `WebAssembly.Module` directly via `setTestCoreWasmModule`, entirely bypassing this glue's normal init flow) assumed. Cloudflare Workers has no global `location` object (browsers/Node do) — a reference to it inside that unexercised code path is the most likely source of "reading 'href' of undefined." **This confirms T022's own disclosed limitation exactly**: *"`grammar-provider.ts` is not yet imported by any reachable route/server-fn... so `bun run build` does not itself bundle/exercise this file — the `?module` import mechanism is proven by T012's spike, not by this build."* T058/T059 (this session) made it reachable for the first time; this is the first time it has ever actually run in a real Worker.
  - **Not fixed this turn** — this is a defect in the approved T012/T022 WASM-loading mechanism itself, not a task-scope item; fixing it means either patching `Parser.init()`'s options further (e.g. explicit `locateFile`) or investigating why the custom `instantiateWasm` doesn't fully prevent the fallback path, which is new diagnostic/implementation work requiring explicit authorization, not a "STOP and report" item that resolves itself.
  - **Practical implication**: Feature 002's extraction *pipeline* (queue orchestration, checkpointing, retry, D1 persistence, status rollup, all query functions) is fully verified live and correct. The one link that is NOT yet functional in production is the actual tree-sitter parse step for real Tier-1 source files — meaning **Feature 002 does not yet produce real symbols against a live deployment**, despite passing 129/129 local tests (which all use `setTestCoreWasmModule`/`setTestGrammarBytesSource` injection, correctly proven not to exercise this exact code path).
  - **Follow-up (same session, explicit user authorization obtained before each step)**: wrote `ensureWorkerLocationPolyfill()` in `grammar-provider.ts` (backfills `self.location`, called once inside `ensureCoreInitialized()` right before `Parser.init()` — does not change the T012/T022 `instantiateWasm` mechanism itself). Verified locally (`tsc` clean, 129/129 tests). **Deployed to production** after explicit user go-ahead. Re-ran live validation against a fresh snapshot (`sindresorhus/is-obj`, snapshotId 4): the `location.href` crash is **confirmed gone** — but a **second, deeper** error now surfaces: `"WebAssembly.instantiate(): Wasm code generation disallowed by embedder"`. This is a documented Cloudflare Workers platform restriction (no dynamic runtime WASM compilation from a fetched byte buffer — only build-time-imported WASM modules may instantiate) that directly conflicts with T022's approved grammar-loading design (`fetchGrammarBytes()` reads grammar bytes from the `ASSETS` binding *at runtime*, then `Language.load(bytes)` compiles them — exactly what Workers blocks). The core runtime WASM is unaffected (loaded via a genuine build-time `?module` static import). **Not fixed** — full details and options in `research.md` §6 risk 4's 2026-09-21 update. This is now a real architecture conflict requiring an explicit decision (e.g., four static per-grammar `?module` imports at build time, which may also require revisiting the `web-tree-sitter@0.25.10` pin from research.md risk 1), not a task-scope item to resolve unilaterally.
  - **Final T064 status**: live infrastructure real and working end-to-end for orchestration; symbol extraction itself remains non-functional in production, with root cause now narrowed twice (both times via real live validation, not speculation) and one architecture-level open question remaining.
  - **Investigation follow-up, same session, research only (no implementation/deploy authorized for this step)**: the architecture conflict has a concrete, real-locally-tested fix — see `research.md` §6 risk 4's 2026-09-21 update. Summary: `Language.load()`'s internal `loadWebAssemblyModule` already supports being handed a pre-compiled `WebAssembly.Module` (skips the disallowed dynamic-compile-from-bytes step entirely) — the public API just never exercises that path. A local experiment substituting a precompiled Module (via monkeypatched `WebAssembly.instantiate`, scoped to one call) produced correct, error-free real parses for all 4 Tier-1 grammars, same pinned `web-tree-sitter@0.25.10`/`tree-sitter-wasms@0.1.13`, no version change. A real `bun run build` confirmed Nitro's `?module` static-import mechanism works generically for our own grammar `.wasm` files (not just `node_modules`), producing the same class of build-time-compiled chunk the already-working core module uses. All test/build artifacts reverted, nothing committed, nothing deployed — this is a documented, ready-to-implement recommendation, not a completed fix.
- [X] T065 [P] Execute quickstart.md's full validation guide end-to-end (all five scenario walkthroughs plus the regression check) and confirm SC-001 through SC-008 are each demonstrably met. **Executed to the extent possible without live Cloudflare infrastructure (T064's blocker) or a browser** — every scenario walkthrough's *logic* is exercised by an automated test, not skipped: US1 (extract → listSymbols → re-run identical) = T017–T020; US2 (`getSymbol` provenance in one call, identity stable across a separate invocation) = T033/T035, T034; US3 (mixed-snapshot `completed_partial` + per-file status) = T038, exact field-for-field match to quickstart's own expected values; US4 (duplicate delivery no-op, mid-unit interrupt never falsely reports done, checkpoint resume) = `symbol-worker.test.ts` (T030); US5 (`listSymbols` never exceeds `limit` across pages) = T054, T056. Regression check: `bunx tsc --noEmit` clean, whole-repo `bun test` 198/198, and `bun run dev` manually confirmed — all 4 routes (`/`, `/catalogue`, `/categories`, `/insights`) HTTP 200, zero new console errors (only the pre-existing unrelated THREE.Clock warning). **Not executed** (disclosed, not silently skipped): the literal `wrangler dev --local` / real Worker isolate restart / `wrangler tail` steps quickstart.md's own "Run" section specifies — same Cloudflare-account blocker as T064.

---

## Phase 9: Architecture Remediation — T022 grammar-loading rewrite (2026-09-21)

**Context**: T064's live Cloudflare validation invalidated T022's approved grammar-loading design (`ASSETS.fetch()` → `Language.load(bytes)`) — Workers rejects runtime WASM compilation from fetched bytes ("Wasm code generation disallowed by embedder"). Full finding and the proven-locally replacement mechanism: `research.md` §6 risk 4 (2026-09-21 updates); architecture decision: `plan.md`'s "Architecture Remediation" section. T022 itself is left checked and unedited as the historical record; this phase supersedes its grammar-byte-sourcing mechanism only. `GrammarProvider.getParser()`'s public signature is unchanged (`contracts/language-grammar-provider.md`).

- [X] T066 **[REMEDIATION, supersedes T022's grammar-byte-sourcing mechanism]** Rewrite `src/lib/code-intel/symbols/grammar-provider.ts`: (1) add four `?module` static imports for `public/wasm/tree-sitter-{java,javascript,typescript,tsx}.wasm`, mirroring the existing `coreWasmModule` import; (2) remove the runtime `ASSETS`-fetch path in full — `fetchGrammarBytes`, `resolveBytesSource`, `AssetsBindingLike`, `getAssetsBinding`, `GrammarBytesSource`, `setTestGrammarBytesSource`; (3) add a small helper that monkeypatches `WebAssembly.instantiate` for the duration of exactly one `Language.load()` call (restored immediately after, success or failure) so the internal bytes-form compile is redirected to the precompiled per-language `Module` — the substitution mechanism proven in research.md §6 risk 4's local experiment; (4) replace `setTestGrammarBytesSource` with an equivalent test-injection hook accepting a precompiled per-language test `Module` (mechanical test-harness change, same rationale as `setTestCoreWasmModule`). No change to `getParser`'s signature, memoization (`languageCache`/`parserCache`), or the unsupported-language defensive throw. Depends on: T022 (supersedes its mechanism, not its file). Blocks: T067. **Implemented 2026-09-21.** All four items done exactly as scoped; `getAssetsBinding`/`fetchGrammarBytes`/`resolveBytesSource`/`AssetsBindingLike`/`GrammarBytesSource`/`GRAMMAR_ASSET_PATH`/`setTestGrammarBytesSource` removed in full, grepped repo-wide to confirm zero remaining references. **One implementation detail surfaced beyond the task's own text, disclosed not hidden**: `Language.load()`'s internal `getDylinkMetadata()` still needs *some* real `Uint8Array` to byte-walk (parses the grammar's dylink custom section for link metadata — a plain header parse, not WASM compilation, so it doesn't hit the Workers restriction) — rather than embedding each multi-MB grammar file a second time as bytes, `buildSyntheticDylinkBytes()` derives a minimal synthetic WASM buffer at runtime (module header + the real dylink section's payload, extracted from the already-compiled `Module` via the standard, compile-free `WebAssembly.Module.customSections()` API) — this buffer is walked for metadata only and is never what gets compiled; the actual compiled code always comes from the build-time-imported `Module` via the `instantiate` substitution. Test hook renamed `setTestGrammarModules(modules)` (per-language `WebAssembly.Module` map, mirrors `setTestCoreWasmModule`'s existing rationale exactly — Bun's `.wasm?module` resolver doesn't behave like Nitro's). **Test-harness dedup, disclosed**: the identical WASM-bootstrap `beforeAll` block was duplicated across 9 test files before this change; consolidated into one new shared helper, `tests/support/wasm-test-modules.ts` (`installTestWasmModules()`), all 9 call sites updated. `bunx tsc --noEmit` clean (one `sections[0]!` non-null assertion needed, safe — guarded by the preceding `length === 0` check). `bun test`: symbols suite 129/129, whole-repo 198/198 — identical to the pre-remediation baseline, zero regressions. `bun run build` succeeds; the four grammar `.wasm` files now appear as build-time-compiled chunks under `.output/server/wasm/tree-sitter-{java,javascript,typescript,tsx}-<hash>.wasm` (same treatment as the core module's own chunk) — confirms the mechanism genuinely changed, not just compiled. `.output/server` totals ≈11 MB (~17% of the 64 MiB cap, comfortably under — full measurement is T067's own scope, this is a same-session sanity check only). T067/T068 not started; not deployed.
- [X] T067 **[REMEDIATION, validation gate 1 — local, required before any deployment]** After T066: `bunx tsc --noEmit` clean; full `bun test` suite green with no regression against the pre-remediation baseline (198 passing); `bun run build` succeeds and the four grammar `.wasm` files appear as build-time-compiled chunks under `.output/server/wasm/` (not `.output/public/wasm/` static assets — confirms the mechanism actually changed, not just compiled); re-measure aggregate Worker-script bundle size against the 64 MiB cap and record the result alongside T063's existing measurement in `research.md` §6 risk 2. This gate must pass in full before T068 is attempted — it is not sufficient by itself to declare the remediation complete (T012/T022's original local-only pass already demonstrated why: this exact class of Workers-sandbox defect is invisible to local/injected tests). Depends on: T066. Blocks: T068. **PASSED, 2026-09-21.** `bunx tsc --noEmit` clean. `bun test` whole-repo 198/198, zero regression against baseline. `bun run build` succeeds; confirmed via direct inspection of `.output/server/index.mjs` that the four grammar chunks are loaded via genuine `await import("./wasm/tree-sitter-<lang>-<hash>.wasm")` (the `?module`/esmImport mechanism, not a static-asset fetch) and that the built server code contains `customSections`/`withPrecompiledInstantiate`-class logic (T066's mechanism is actually reachable/bundled, not tree-shaken away) — grepped repo-wide for `fetchGrammarBytes`/`GRAMMAR_ASSET_PATH`/`assets.internal` in `.output/server`: zero matches (the only remaining `ASSETS` reference in the built output is Nitro's own unrelated generic static-asset router, pre-existing, not grammar-specific). Bundle size re-measured and recorded in `research.md` §6 risk 2's T067 follow-up: `.output/server` ≈10.52 MB, 16.44% of the 64 MiB cap (up from T063's ~8%, exactly the expected ≈5.8 MB shift from "static asset" to "build-time chunk" reclassification — cap not threatened). `public/wasm/*.wasm` confirmed to still exist as unreferenced static assets (harmless dead weight, disclosed in T066's own notes, not this gate's concern). Build artifacts (`.output/`) removed after inspection, not committed (gitignored). Not deployed; T068 not started.
- [X] T068 **[REMEDIATION, validation gate 2 — live Cloudflare, required before declaring remediation complete]** Only after T067 passes and with explicit user authorization for deployment (not implied by T067 passing, per this session's operating rules): deploy the rewritten `grammar-provider.ts`; re-run `extractSnapshotSymbols` against a real snapshot covering all four Tier-1 languages; confirm via direct `wrangler d1 execute --remote` inspection of `file_extractions` (not just `getExtractionStatus`'s summary, per T064's own established diagnostic method) that every Tier-1 file reaches `status = 'extracted'` (not `failed`) with real, non-empty `symbols` rows. Record the result in `research.md` §6 risk 4 as the closing update. The remediation is not complete until this task passes. Depends on: T067. **PASSED, 2026-09-21.** Deployed `bunx nitro deploy --prebuilt` (Version `301059b7-8a5f-443e-9c4f-a10a0d18ac91`, then `17e46c74-bc86-46c6-9d15-cb031e0c8c9c` after a `SYMBOL_EXTRACTOR_VERSION` bump — see below — then `39ca3c25-09e4-48ad-9e08-c46b3bd381d0` after a concurrent unrelated Feature 001 `GITHUB_TOKEN` fix landed in the working tree, authored by a peer session, not this one). **Blocker encountered and resolved, with explicit user authorization at each step (not worked around unilaterally)**: (1) the 4 already-completed Feature 001 snapshots (ids 2-4) were stuck on `reused: true` under unchanged `SYMBOL_EXTRACTOR_VERSION="v1"` (FR-013 idempotency correctly refusing to re-parse) — user explicitly authorized bumping it to `"v2"` (research.md §10's own established mechanism for exactly this class of fix, not a new architecture), which required a small, disclosed test fix (`tests/integration/symbols/extract-snapshot-symbols.test.ts` — 6 assertions hardcoded literal `"v1"` for what should be "the current version"; changed to import `SYMBOL_EXTRACTOR_VERSION`, kept the deliberately-old `"v0-old"` sentinel as-is); (2) acquiring fresh Java/TSX snapshots initially failed live with `SNAPSHOT_REPOSITORY_UNAUTHORIZED` across 3 different public repos — root-caused to `process.env.GITHUB_TOKEN` not resolving in the deployed Worker (Feature 001's `github-content-provider.ts`/`github-fetch.ts`); **not fixed by this task** — a peer session fixed it independently (`getGitHubToken()` helper in `persistence/cloudflare-env.ts`, reading the binding directly) while this task was running; verified sound (`tsc` clean, 198/198) before relying on it, then rebuilt/redeployed to pick it up. **Live extraction results, all real, all via `extractSnapshotSymbols` against genuinely completed Feature 001 snapshots, verified by direct `wrangler d1 execute --remote` inspection (not `getExtractionStatus` alone)**: snapshot 2 (`sindresorhus/pify`), 3 (`sindresorhus/is-plain-obj`), 4 (`sindresorhus/is-obj`) — JS+TS, re-extracted fresh under v2; snapshot 5 (`Hello-World-EE/Java-Hello-World-Enterprise-Edition`, commit `0574892e8176f8f67b43bd2e1992a3dee83203f8`) — Java, freshly acquired+extracted; snapshot 6 (`telegram-mini-apps-dev/vite-boilerplate`, commit `c74f374d7f8d1e719aaeae0214bca5410b1c462c`) — TSX+TS, freshly acquired+extracted. **Aggregate, all 5 snapshots, `extractor_version='v2'`**: 33 files `extracted` (java 14, javascript 9, typescript 8, tsx 2), 65 `skipped_unsupported` (legitimately non-Tier-1: `.md`/`.json`/`.class`/IDE files/etc.), **zero `failed`**. 77 real symbols persisted (java: 10 class + 4 interface + 23 method; javascript: 13 function + 9 method; typescript: 1 class + 1 function + 2 method; tsx: 1 function). All 5 `snapshot_extractions` rows reached terminal `completed_partial` (correct per FR-016 — every skip is a legitimate non-Tier-1 file, not a parse failure). Confirmed no `ASSETS.fetch()` grammar path reintroduced: grepped the deployed `.output/server` for `fetchGrammarBytes`/`GRAMMAR_ASSET_PATH`/`setTestGrammarBytesSource` — zero matches; confirmed `index.mjs` loads all four grammars via genuine `await import("./wasm/tree-sitter-<lang>-<hash>.wasm")`. **Feature 002 is now production-complete for its Tier-1 scope** — real, deployed, live-verified symbol extraction for Java, JavaScript, TypeScript, and TSX, with persisted D1 evidence, closing research.md §6 risk 4 for good.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — starts immediately.
- **Foundational (Phase 2)**: Depends on Setup. **Includes the three feasibility spikes (T012–T014), which BLOCK all user-story implementation tasks that touch grammar loading, bundle size, or large-file handling** — explicitly ordered before Phase 3 begins, per the task-generation requirement that spikes precede dependent implementation.
- **User Story 1 (Phase 3)**: Depends on Foundational (including spikes). No dependency on any other user story — this is the MVP.
- **User Story 2 (Phase 4)**: Depends on Foundational + US1's `extraction-pipeline`/`symbol-d1-client` existing (symbols must be extractable before they're queryable by identity), and shares `symbol.functions.ts` with US1 (same file, sequential edit).
- **User Story 3 (Phase 5)**: Depends on Foundational + US1's `extraction-pipeline.ts`/`symbol-worker.ts` (extends the same pipeline with failure/skip paths) and T014's spike.
- **User Story 4 (Phase 6)**: Depends on Foundational + US1's `symbol-worker.ts`/`extraction-pipeline.ts` (extends the same worker with idempotency/retry/checkpoint logic) and US3's status rollup (T042).
- **User Story 5 (Phase 7)**: Depends on Foundational + US1/US2/US3's handlers existing in `symbol.functions.ts` (T032, T035, T043, T044) — the registration task (T058) is necessarily last among the five server functions.
- **Polish (Phase 8)**: Depends on all five user stories being complete.
- **Architecture Remediation (Phase 9)**: Depends on T064's live-validation finding (already complete) and T022 (supersedes its mechanism only, not its file/task record). Strictly sequential: T066 → T067 → T068 — each gate blocks the next, no parallel opportunity within this phase. Independent of Polish (Phase 8) otherwise; both phases' prerequisites are already satisfied.

### Within Each User Story

- Tests are written first and MUST fail before their corresponding implementation task closes.
- Domain/query-file tasks before pipeline tasks; pipeline before worker; worker before server function; server function before registration (US5).

### Parallel Opportunities

- Setup: T002, T003 in parallel (T001 first — later tasks assume the dependency exists).
- Foundational: T005, T006, T007 in parallel after T004; T008a in parallel with T005–T007 (independent file); T009 in parallel with T005–T008a; T013, T014 in parallel with each other and with T005–T009 (T012 is sequential-only, since T013/T014 don't depend on its outcome, but T022 in Phase 3 does).
- US1: T015–T020 (all tests) in parallel; T023–T026 (all four `.scm` query files) in parallel.
- US2: T033, T034 in parallel.
- US3: T036–T038 (tests) in parallel.
- US4: T045–T047 (tests) in parallel.
- US5: T054, T055 in parallel.
- Polish: T060 and T065 in parallel with the sequential T061→T062→T063→T064 chain (each depends on the previous succeeding).

---

## Parallel Example: User Story 1

```bash
# Tests, launched together:
Task: "Contract test: detectLanguage determinism in tests/contract/symbols/language-detector.test.ts"
Task: "Contract test: symbol_key hashing determinism in tests/contract/symbols/symbol-identity.test.ts"
Task: "Contract test: Java fixture extraction in tests/contract/symbols/java-extraction.test.ts"
Task: "Contract test: JavaScript fixture extraction in tests/contract/symbols/javascript-extraction.test.ts"
Task: "Contract test: TypeScript/TSX fixture extraction in tests/contract/symbols/typescript-extraction.test.ts"

# Query files, launched together (after T022's spike-informed grammar-provider exists):
Task: "Write java.scm in src/lib/code-intel/symbols/queries/java.scm"
Task: "Write javascript.scm in src/lib/code-intel/symbols/queries/javascript.scm"
Task: "Write typescript.scm in src/lib/code-intel/symbols/queries/typescript.scm"
Task: "Write tsx.scm in src/lib/code-intel/symbols/queries/tsx.scm"
```

---

## Implementation Strategy

### MVP First (User Stories 1 + 2 — both P1)

1. Complete Phase 1: Setup.
2. Complete Phase 2: Foundational, **including all three spikes** — this is the hard gate.
3. Complete Phase 3: User Story 1 — extraction pipeline exists, symbols persist.
4. Complete Phase 4: User Story 2 — symbols are identifiable and traceable.
5. **STOP and VALIDATE**: run quickstart.md's US1/US2 scenarios independently.
6. Deploy/demo if ready — this is the smallest slice that delivers "structural foundation for the Engineering Relationship Graph" as the spec's Primary Objective states it.

### Incremental Delivery

1. Setup + Foundational (with spikes) → foundation ready.
2. US1 → US2 → validate independently → this is the MVP.
3. US3 → real-world heterogeneous-repository safety → validate independently.
4. US4 → production-scale safety (checkpoint/idempotency) → validate independently.
5. US5 → HTTP-reachable queryability for the next feature → validate independently.
6. Polish → whole-repo regression + live Cloudflare validation, mirroring Feature 001's closure.

---

## Requirements Coverage Cross-Reference

Every spec.md Functional Requirement maps to at least one task above:

- FR-001–FR-003 (language detection/grammar selection/unsupported-language outcome): T021, T036, T039
- FR-004, FR-005, FR-007 (deterministic parsing, five symbol kinds, file-scoped failures): T022 (historical grammar-provider implementation); remediated by T066–T068, pending T068 (production satisfaction pending live Cloudflare validation), T028, T029
- FR-006 (directory nodes recorded as addressable structural nodes): T004 (schema), T008 (`getOrCreateDirectory` primitive), **T029 (actually populates `directories` per file — remediated 2026-09-20, previously an unused-primitive gap, see data-model.md Directory entity note)**
- FR-008 (stable identity, scoped to one `extractor_version` — see spec.md Assumptions clarification), FR-013 (determinism): T016, T027, T034, T046
- FR-020 also covers idempotency for duplicate queue delivery specifically (distinct from FR-013's re-extraction determinism): T045, T050
- FR-009, FR-010 (source ranges, parent/child hierarchy): T028
- FR-011 (evidence state always EXTRACTED): T005 domain type + T004 schema `CHECK` constraint
- FR-012 (D1 persistence, no graph DB): T004, T008
- FR-014 (extractor/grammar version provenance): T007, T027, T047
- FR-015 (refuse extraction on non-completed snapshot): T032
- FR-016, FR-017 (snapshot/file-level extraction status): T042, T039, T040, T043, T044
- FR-018 (failed file contributes zero symbols): T040
- FR-019, FR-020, FR-021 (bounded queue units, idempotency, concurrent-request dedup): T030, T050, T052
- FR-022 (snapshot immutability, read-only): T029 (reads via existing `getObject` only)
- FR-023 (full provenance in one lookup): T035, T008
- FR-024, FR-025 (paginated/filterable listing, single-symbol detail): T057, T035
- FR-026, FR-027 (no Feature 001 / existing-atlas regression): T061, T062
